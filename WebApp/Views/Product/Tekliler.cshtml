
@using X.PagedList;
@using X.PagedList.Mvc.Core;
@model List<Entities.Dtos.ShippingProduct>
@{
    Layout = Layout;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Tekliler</h4>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Model Kodu</th>
                            <th>Model Adı</th>
                            <th>Rengi</th>
                            <th>Adet</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            if (Model.Count > 0)
                            {
                                foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.ModelCode</td>
                                        <td>@item.ProductName</td>
                                        <td>@item.ColorName</td>
                                        <td>@item.Count</td>
                                        <td><a class="btn btn-success" href="@Url.Action("TekliDuzenle","Product",new{tekliId=item.TekliId})" style="margin:5px">Düzenle</a><a class="btn btn-danger" href="@Url.Action("DeleteTekli","Product",new{tekliId=item.TekliId})">Sil</a> </td>
                                    </tr>
                                }
                            }
                        }
                      @*  <tr>
                            <td>
                                @{
                                    var productCounts2 = ViewBag.ProductCount;
                                    if (productCounts2 >= 31 && 45<= productCounts2)
                                    {
                                        productCounts2 = 3;
                                    }
                                    else if (productCounts2 >45)
                                    {
                                        productCounts2 = productCounts2 / 15;

                                    }
                                    else if (productCounts2 <=15)
                                    {
                                        productCounts2 = 1;
                                    }
                                    else if (productCounts2 > 15 && productCounts2 <= 30)
                                    {
                                        productCounts2 = 2;
                                    }
                                }
                                <div class="btn-group" role="group" aria-label="Basic example">
                                  
                                    @for (int b = 0; b < productCounts2; b++)
                                    {
                                        <a class="btn btn-outline-secondary"  style="widht:10%" href="@Url.Action("Tekliler","Product",new{page=b+1})">@(b + 1)</a>
                                    }

                                </div>
                            </td>
                        </tr>*@
                    @*  <tr>
                            <td>
                                <div class="btn-group" role="group" aria-label="Basic example">
                                  
                                       @Html.PagedListPager((IPagedList)Model,page=>Url.Action("Tekliler",new{page}),
                                    new X.PagedList.Web.Common.PagedListRenderOptions {
                                    DisplayLinkToIndividualPages = true,
                                    DisplayPageCountAndCurrentLocation = false,
                                    MaximumPageNumbersToDisplay = 10,
                                    LiElementClasses = new string[] { "page-item" },
                                    PageClasses = new string[] { "page-link" }})
                                </div>
                            </td>

                        </tr>*@

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
































@*@using X.PagedList;
@using X.PagedList.Mvc.Core;
@model IPagedList<Entities.Dtos.ShippingProduct>
@{
    Layout = Layout;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Tekliler</h4>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Model Kodu</th>
                            <th>Model Adı</th>
                            <th>Rengi</th>
                            <th>Adet</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model!=null)
                        {
                            if (Model.Count> 0)
                            {
                                foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.ModelCode</td>
                                        <td>@item.ProductName</td>
                                        <td>@item.ColorName</td>
                                        <td>@item.Count</td>
                                        <td><a class="btn btn-success" href="@Url.Action("TekliDuzenle","Product",new{tekliId=item.TekliId})" style="margin:5px">Düzenle</a><a class="btn btn-danger" href="@Url.Action("DeleteTekli","Product",new{tekliId=item.TekliId})">Sil</a> </td>
                                    </tr>
                                }
                            }
                        }
                        <tr>
                            <td>
                                <div class="btn-group" role="group" aria-label="Basic example">
                                    @* @for (int b =0; b < productCounts; b++)
                                    {
                                    <a class="btn btn-outline-secondary" href="@Url.Action("AddShippingDetail","Shipping",new{page=b+1})">@(b+1)</a>
                                    }*@

                                   @* @Html.PagedListPager((IPagedList)Model,page=>Url.Action("Tekliler",new{page}),
                                    new X.PagedList.Web.Common.PagedListRenderOptions {
                                    DisplayLinkToIndividualPages = true,
                                    DisplayPageCountAndCurrentLocation = false,
                                    MaximumPageNumbersToDisplay = 10,
                                    LiElementClasses = new string[] { "page-item" },
                                    PageClasses = new string[] { "page-link" }})
                                </div>
                            </td>

                        </tr>
                      
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>*@