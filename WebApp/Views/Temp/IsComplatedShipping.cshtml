@model List<Entities.Dtos.ShippingProduct>


<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Eklenen Ürünler</h4>
            <div class="col-12 row">

                <div class="col-10" style="width:80%">
                </div>
                <div class="col-2" style="width:20%">
                    <button onclick="SiparisOnayi2()" type="button" class="btn btn-secondary btn-rounded btn-fw">Siparişi Kaydet</button>
                </div>
            </div>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                Model Kodu
                            </th>
                            <th>
                                Model Adı
                            </th>
                            <th>
                                Renk
                            </th>
                            <th>
                                Adet
                            </th>

                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int i = 1;
                        }
                        @Html.Hidden("ShippingCountScript",Model.Count)
                        @foreach (var item in Model)
                        {


                        //***////////////////////


                            @*   siparişlerimde hala görünüyor. engellemek için siparişi tamam la dediğinde toplam idler,shippingıD color ve productları gönderip
                        shipping detail sayfasında Iscompalted ve finis true olmalı*@

                            //***////////////////////
                            //***////////////////////
                            if (item.Count > 0)
                            {
                                @Html.Hidden("shippingId",item.ShippingId)
                        <tr>
                            @*  <td class="py-1">
        <img src="../../images/faces/face1.jpg" alt="image">
        </td>*@
                            <td>
                                @*          @Html.Hidden("productIdComplated_"+i,item.ProductId)*@
                                @item.ModelCode
                            </td>
                            <td>
                                <input type="text" id="productIdComplated_@i" value="@item.ProductId" hidden>
                                @*          @Html.Hidden("productIdComplated_"+i,item.ProductId)*@
                                @item.ProductName
                            </td>
                            @*   <td>
        <div class="progress">
        <div class="progress-bar bg-success" role="progressbar" style="width: 25%" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100"></div>
        </div>
        </td>*@
                            <td>
                                <input type="text" id="colorNameComplated_@i" value="   @item.ColorName" hidden>
                                @*      @Html.Hidden("colorNameComplated_"+i,item.ColorName)*@
                                <input type="text" id="colorIdComplated_@i" value="@item.ColorId" hidden>
                                @*   @Html.Hidden("colorIdComplated_"+i,item.ColorId)*@
                                @item.ColorName
                            </td>
                            <td>
                                <input type="text" id="colorCountComplated_@i" value=" @item.Count" hidden>
                                @*    @Html.Hidden("colorCountComplated_"+i,item.Count)*@
                                @item.Count
                            </td>
                            <td>
                                @*   <button type="button" class="btn btn-warning btn-xs legitRipple pull-right" onclick="Delete('@item.ProductId','@item.ColorId','@item.ShippingId','@item.Count')">
                Sil
            </button>*@
                                <a class="btn btn-warning btn-xs legitRipple pull-right" href="@Url.Action("Delete","Temp",new{tempId=item.TempId,count=item.Count})"> Sil</a>
                            </td>
                        </tr>
                                i++;
                            }
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>





